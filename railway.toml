[build]
builder = "railpack"
buildCommand = "echo 'no build needed'"

[deploy]
startCommand = "python3 build_all.py --only db --upload --serve"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
